def is_prime():
    numero: int = int(input("Introduce un número entero: "))

    if numero > 1:
        for i in range(2, int(numero)):
            if (int(numero) % i) == 0:
                return False
                break
            else:
                return True

print({is_prime()})
